# LLVM-and-more v2 roadmap

The v2 foundation separates output-line parsing from process control and adds a
compiler-warning test harness. Next work is a token stream, source locations,
and an AST-backed evaluator.
